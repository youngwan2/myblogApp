import styles from './footer.module.css'

const Footer = () => {
  return (
    <footer className={styles.main_footer}>
      <p>{"CREATE BLOG APP | All Right Reserved"}</p>
    </footer>
  );
};

export default Footer;
